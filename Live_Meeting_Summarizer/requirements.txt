# Core app
streamlit>=1.32.0
numpy
scipy
soundfile
python-dotenv
bcrypt

# Audio (browser-based recording — no sounddevice needed)
audio-recorder-streamlit
librosa

# Speech-to-text (CPU-only torch to keep image size manageable)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchaudio==2.3.1+cpu
openai-whisper

# Summarisation (PyTorch backend only, avoids heavy TensorFlow)
transformers==4.41.2
sentencepiece
accelerate

# Analytics & export
matplotlib
scikit-learn
textblob
fpdf2

# Misc
protobuf>=3.20,<6
